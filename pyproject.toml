[project]
name = "lottery-539"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "requests>=2.33.1",
    "rich>=14.3.3",
]

[dependency-groups]
dev = [
    "pytest>=9.0.3",
]
